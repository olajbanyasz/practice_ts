/*
FP uses pure functions, immutability, and higher-order functions to create predictable and reusable code.
In the functionalProgramming.ts file, we see examples of pure functions (add, multiply), immutability (ReadonlyArray), and higher-order functions (mapArray, curry).
OOP uses classes, objects, and inheritance to model real-world entities and their interactions.
In the objectOrientedProgramming.ts file, we see examples of classes (Person), objects (person1), and inheritance (Employee extends Person).
*/
